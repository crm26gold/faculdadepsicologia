-- Version aligned with the remote migration history (UTC).
-- Internal event trigger is not a client API. Keep the trigger itself intact.
do $$
begin
  if to_regprocedure('public.rls_auto_enable()') is not null then
    revoke execute on function public.rls_auto_enable() from public, anon, authenticated;
  end if;
end;
$$;
